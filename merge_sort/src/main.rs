//implment mergesort
use bigdecimal::BigDecimal;

struct Data {
    id: String,
    value: BigDecimal,
    diff: BigDecimal,
}

impl Data {
    fn new(
        id: String,
        value: BigDecimal,
        diff: BigDecimal,
    ) -> Self {
        Self {
            id,
            value,
            diff
        }
    }
    
}


pub merge() -> Result<()> {
    
}

fn main() {
    
    println!("Hello, world!");
}
